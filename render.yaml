services:
  - type: web
    name: lm-scan-compliance
    env: python
    buildCommand: "python3 -c 'import db; db.init_db()'"
    startCommand: "python3 server.py"
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false
